import type{LeadTask}from'../tasks/types';import type{LeadAppointment}from'../appointments/types';export type MyWork={tasks_today:number;tasks_overdue:number;appointments_today:number;appointments_upcoming:number;leads_need_follow_up:number;completed_tasks_today:number;today_tasks:LeadTask[];overdue_tasks:LeadTask[];upcoming_appointments:LeadAppointment[];follow_up_leads:Array<{id:string;code:string;full_name:string;next_follow_up_at:string}>};export type TeamWork={team_tasks_today:number;team_tasks_overdue:number;team_appointments_today:number;team_leads_need_follow_up:number;per_user_summary:Array<{user:{id:string;full_name:string;email:string};leads_owned:number;tasks_today:number;tasks_overdue:number;appointments_today:number;completed_today:number;leads_need_follow_up:number}>};
