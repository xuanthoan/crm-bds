import type {LeadUser} from '../leads/types';export type MyWorkSummary={tasks_today:number;tasks_overdue:number;appointments_today:number;appointments_upcoming:number;leads_need_follow_up:number;completed_tasks_today:number};export type TeamUserSummary=MyWorkSummary&{user:LeadUser;leads_owned:number};export type TeamWorkSummary={team_tasks_today:number;team_tasks_overdue:number;team_appointments_today:number;team_leads_need_follow_up:number;per_user_summary:TeamUserSummary[]};
