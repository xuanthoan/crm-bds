import type {DashboardPreset} from './types';
const PRESETS:{value:DashboardPreset;label:string}[]=[{value:'today',label:'Hôm nay'},{value:'last_7_days',label:'7 ngày qua'},{value:'last_30_days',label:'30 ngày qua'},{value:'this_month',label:'Tháng này'},{value:'last_month',label:'Tháng trước'},{value:'custom',label:'Tùy chọn'}];
export function DashboardDateRangeFilter({preset,fromDate,toDate,onChange}:{preset:DashboardPreset;fromDate:string;toDate:string;onChange:(next:{preset:DashboardPreset;fromDate:string;toDate:string})=>void}){return <div className="dashboard-filter"><div className="filter-pills">{PRESETS.map(p=><button key={p.value} type="button" className={preset===p.value?'active':''} onClick={()=>onChange({preset:p.value,fromDate,toDate})}>{p.label}</button>)}</div>{preset==='custom'&&<div className="custom-range"><label>Từ ngày<input type="date" value={fromDate} onChange={e=>onChange({preset,fromDate:e.target.value,toDate})}/></label><label>Đến ngày<input type="date" value={toDate} onChange={e=>onChange({preset,fromDate,toDate:e.target.value})}/></label></div>}</div>}
