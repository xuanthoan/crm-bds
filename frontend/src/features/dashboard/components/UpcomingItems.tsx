import{navigateTo}from'../../../routes/AppRoutes';export function UpcomingItems({title,items}:{title:string;items:Array<{id:string;title?:string;lead:{id:string;code:string;full_name:string};due_at?:string;start_at?:string}>}){return <article className="detail-card"><h2>{title}</h2>{items.length?items.map(x=><button key={x.id} className="dashboard-row" onClick={()=>navigateTo(`/leads/${x.lead.id}`)}><span><strong>{x.title}</strong><small>{x.lead.code} · {x.lead.full_name}</small></span><time>{new Date(x.due_at??x.start_at??'').toLocaleString('vi-VN')}</time></button>):<p className="hint">Không có dữ liệu.</p>}</article>}
