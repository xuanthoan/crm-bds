import{useState}from'react';import{Modal}from'../../components/Modal';import{FormError}from'../../components/FormError';import{formatApiError}from'../../services/apiClient';import{changePropertyStatus}from'./api';import{INVENTORY_STATUS_LABELS}from'./constants';import type{PropertyUnit}from'./types';export function PropertyStatusModal({property,onClose,onSaved}:{property:PropertyUnit;onClose:()=>void;onSaved:()=>void}){const[status,setStatus]=useState(property.inventory_status);const[note,setNote]=useState('');const[errors,setErrors]=useState<string[]>([]);return <Modal title="Đổi trạng thái kho hàng" onClose={onClose}><form className="lead-form" onSubmit={async e=>{e.preventDefault();try{await changePropertyStatus(property.id,{inventory_status:status,note:note.trim()||null});onSaved()}catch(err){setErrors(formatApiError(err))}}}><FormError messages={errors}/><label>Trạng thái *<select value={status} onChange={e=>setStatus(e.target.value)}>{Object.entries(INVENTORY_STATUS_LABELS).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><div className="workflow-hint-box full-span"><strong>Hướng dẫn nhanh</strong><ul><li><b>Còn hàng:</b> BĐS có thể tạo booking/deal mới.</li><li><b>Giữ chỗ:</b> BĐS đang được giữ chỗ tạm thời.</li><li><b>Đang đàm phán:</b> BĐS đang trong quá trình trao đổi/chưa chốt.</li><li><b>Đã cọc:</b> Đã có booking đặt cọc.</li><li><b>Đã bán:</b> Đã có hợp đồng/giao dịch hoàn tất.</li><li><b>Đã khóa / Không khả dụng:</b> Không cho giao dịch tạm thời hoặc không được bán.</li><li><b>Lưu ý:</b> Giữ chỗ/Đã cọc/Đã bán nên ưu tiên để hệ thống tự cập nhật; admin chỉ đổi thủ công khi thật sự cần điều chỉnh dữ liệu.</li></ul></div><label>Ghi chú<textarea value={note} onChange={e=>setNote(e.target.value)}/></label><footer className="modal-actions"><button type="button" className="secondary-button" onClick={onClose}>Hủy</button><button>Lưu trạng thái</button></footer></form></Modal>}
