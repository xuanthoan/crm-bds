import type{Contract}from'../types';const m=(v:number)=>new Intl.NumberFormat('vi-VN',{style:'currency',currency:'VND',maximumFractionDigits:0}).format(Math.max(v,0));export function ContractSummaryCard({contract:c}:{contract:Contract}){return <div className="summary-grid"><article><span>Giá trị hợp đồng</span><strong>{m(c.contract_value)}</strong></article><article><span>Tiền cọc</span><strong>{m(c.deposit_amount)}</strong></article><article><span>Tổng đã thanh toán</span><strong>{m(c.total_paid)}</strong></article><article><span>Còn lại</span><strong>{m(c.remaining_amount)}</strong></article></div>}
