import{CONTRACT_STATUS_LABELS,CONTRACT_TYPE_LABELS}from'../constants';export function ContractFilters({filters,onChange}:{filters:Record<string,string>;onChange:(v:Record<string,string>)=>void}){return <div className="filters"><input placeholder="Tìm mã, số hợp đồng, khách hàng, bất động sản" value={filters.q||''} onChange={e=>onChange({...filters,q:e.target.value})}/><select value={filters.status||''} onChange={e=>onChange({...filters,status:e.target.value})}><option value="">Tất cả trạng thái</option>{Object.entries(CONTRACT_STATUS_LABELS).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select><select value={filters.contract_type||''} onChange={e=>onChange({...filters,contract_type:e.target.value})}><option value="">Tất cả loại</option>{Object.entries(CONTRACT_TYPE_LABELS).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></div>}
