import type{ContractActivity}from'../types';export function ContractTimeline({items}:{items:ContractActivity[]}){return <div className="timeline">{items.map(a=><article key={a.id}><strong>{a.activity_label}</strong>{a.old_value&&a.new_value&&<p>{a.old_value} → {a.new_value}</p>}<p>{a.content||'Chưa cập nhật'}</p><small>Người thực hiện: {a.actor.full_name} · {new Date(a.created_at).toLocaleString('vi-VN')}</small></article>)}</div>}
