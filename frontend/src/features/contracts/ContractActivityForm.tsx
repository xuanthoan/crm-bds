import{useState}from'react';import{addContractActivity}from'./api';export function ContractActivityForm({contractId,onSaved}:{contractId:string;onSaved:()=>void}){const[content,setContent]=useState('');return <form onSubmit={async e=>{e.preventDefault();await addContractActivity(contractId,{title:'Ghi chú',content});setContent('');onSaved()}}><textarea value={content} onChange={e=>setContent(e.target.value)} placeholder="Thêm ghi chú"/><button>Thêm hoạt động</button></form>}
