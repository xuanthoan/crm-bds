import{useState}from'react';import{Modal}from'../../components/Modal';import{createContract,updateContract}from'./api';import type{Contract}from'./types';export function ContractFormModal({contract,dealId,onClose,onSaved}:{contract?:Contract;dealId?:string;onClose:()=>void;onSaved:()=>void}){const[deal,setDeal]=useState(dealId||contract?.deal_id||'');const[value,setValue]=useState(String(contract?.contract_value||''));const[number,setNumber]=useState(contract?.contract_number||'');const[error,setError]=useState('');return <Modal title={contract?'Sửa hợp đồng':'Tạo hợp đồng'} onClose={onClose}><form onSubmit={async e=>{e.preventDefault();try{const p={deal_id:deal,contract_value:Number(value),contract_number:number||null};contract?await updateContract(contract.id,p):await createContract(p);onSaved()}catch(x){setError(x instanceof Error?x.message:'Không thể lưu hợp đồng')}}}><label>Giao dịch<input required value={deal} onChange={e=>setDeal(e.target.value)}/></label><label>Giá trị hợp đồng<input required type="number" min="1" value={value} onChange={e=>setValue(e.target.value)}/></label><label>Số hợp đồng<input value={number} onChange={e=>setNumber(e.target.value)}/></label>{error&&<p className="form-error">{error}</p>}<div className="modal-actions"><button type="button" onClick={onClose}>Hủy</button><button>Lưu</button></div></form></Modal>}
