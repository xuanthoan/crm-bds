import { useState } from 'react';import { Modal } from '../../components/Modal';import { FormError } from '../../components/FormError';import { formatApiError } from '../../services/apiClient';import { updateCustomerStatus } from './api';import { CUSTOMER_STATUS_LABELS } from './constants';import type { Customer,CustomerStatus } from './types';
export function CustomerStatusModal({customer,onClose,onSaved}:{customer:Customer;onClose:()=>void;onSaved:()=>void}){const [value,setValue]=useState<CustomerStatus>(customer.status);const [note,setNote]=useState('');const [errors,setErrors]=useState<string[]>([]);async function submit(e:any){e.preventDefault();try{await updateCustomerStatus(customer.id,value,note);onSaved()}catch(err){setErrors(formatApiError(err))}}return <Modal title="Đổi trạng thái khách hàng" onClose={onClose}><form className="admin-form" onSubmit={submit}><label>Trạng thái<select value={value} onChange={e=>setValue(e.target.value as CustomerStatus)}>{Object.entries(CUSTOMER_STATUS_LABELS).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label>Ghi chú<textarea value={note} onChange={e=>setNote(e.target.value)}/></label><FormError messages={errors}/><footer className="modal-actions"><button type="button" className="secondary-button" onClick={onClose}>Hủy</button><button>Lưu</button></footer></form></Modal>}
