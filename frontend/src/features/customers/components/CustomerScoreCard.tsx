import {SCORE_LABELS} from '../constants';import type {Customer} from '../types';
export function CustomerScoreBadge({label,score}:{label:string|null;score?:number}){const key=label??'unqualified';return <span className={`badge score-${key}`}>{SCORE_LABELS[key as keyof typeof SCORE_LABELS]??'Chưa đủ điều kiện'}{score!==undefined?` · ${score}`:''}</span>}
export function CustomerScoreCard({customer}:{customer:Customer}){return <section className="detail-card score-card"><h2>Điểm khách hàng</h2><strong className="score-total">{customer.score_total}</strong><CustomerScoreBadge label={customer.score_label}/><dl className="info-grid"><div><dt>Cập nhật lúc</dt><dd>{customer.score_updated_at?new Date(customer.score_updated_at).toLocaleString('vi-VN'):'Chưa cập nhật'}</dd></div><div><dt>Ghi chú điểm</dt><dd>{customer.score_note||'Chưa cập nhật'}</dd></div></dl></section>}
