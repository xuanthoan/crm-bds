import type { CustomerActivity } from '../types';
export function CustomerTimeline({activities=[]}:{activities?:CustomerActivity[]}){if(!activities.length)return <p className="empty-state">Chưa có hoạt động khách hàng.</p>;return <div className="lead-timeline">{activities.map(a=><article className="timeline-item" key={a.id}><span className="timeline-marker"/><div><header><strong>{a.title}</strong><time>{new Date(a.created_at).toLocaleString('vi-VN')}</time></header><small>{a.user?.full_name??'Hệ thống'} · {a.activity_type}</small>{a.content&&<p>{a.content}</p>}{(a.old_value||a.new_value)&&<span className="value-change">{a.old_value??'—'} → {a.new_value??'—'}</span>}</div></article>)}</div>}
