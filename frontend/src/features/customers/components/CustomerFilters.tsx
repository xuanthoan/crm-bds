import { CUSTOMER_STATUS_LABELS, CUSTOMER_TYPE_LABELS } from '../constants';
export function CustomerFilters({filters,onChange,onSearch}:{filters:any;onChange:(v:any)=>void;onSearch:()=>void}){return <div className="filter-panel lead-filters"><input placeholder="Tên, điện thoại, email, mã KH" value={filters.search} onChange={e=>onChange({...filters,search:e.target.value})}/><select value={filters.status} onChange={e=>onChange({...filters,status:e.target.value})}><option value="">Mọi trạng thái</option>{Object.entries(CUSTOMER_STATUS_LABELS).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select><select value={filters.customer_type} onChange={e=>onChange({...filters,customer_type:e.target.value})}><option value="">Mọi loại</option>{Object.entries(CUSTOMER_TYPE_LABELS).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select><input placeholder="Nguồn" value={filters.source} onChange={e=>onChange({...filters,source:e.target.value})}/><input placeholder="Dự án" value={filters.project} onChange={e=>onChange({...filters,project:e.target.value})}/><button onClick={onSearch}>Lọc</button></div>}
