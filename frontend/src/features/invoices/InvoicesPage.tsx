import{useEffect,useState}from'react';import{navigateTo}from'../../routes/AppRoutes';import{listInvoices}from'../payments/api';import type{PaymentInvoice}from'../payments/types';const money=(v:number)=>new Intl.NumberFormat('vi-VN',{style:'currency',currency:'VND',maximumFractionDigits:0}).format(v);export function InvoicesPage(){const[items,setItems]=useState<PaymentInvoice[]>([]);const[q,setQ]=useState('');const[status,setStatus]=useState('');useEffect(()=>{void load()},[]);async function load(){setItems((await listInvoices({q,status})).data)}return <div className="admin-page"><header className="page-header"><div><h1>Hóa đơn</h1><p>Quản lý hóa đơn và chứng từ thanh toán.</p></div></header><div className="filter-bar"><input placeholder="Tìm mã hóa đơn, hợp đồng, khách hàng, SĐT" value={q} onChange={e=>setQ(e.target.value)}/><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">Tất cả trạng thái</option><option value="draft">Nháp</option><option value="issued">Đã phát hành</option><option value="cancelled">Đã hủy</option></select><button onClick={load}>Tìm kiếm</button></div><div className="table-card"><table><thead><tr><th>Mã hóa đơn</th><th>Hợp đồng</th><th>Khách hàng</th><th>Số tiền</th><th>Ngày phát hành</th><th>Trạng thái</th><th></th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td>{i.invoice_code}</td><td>{i.contract?.contract_code||'—'}</td><td>{i.customer?.full_name||'—'}</td><td>{money(i.amount)}</td><td>{i.issued_date?new Date(i.issued_date).toLocaleDateString('vi-VN'):'Chưa phát hành'}</td><td>{i.status_label}</td><td><button onClick={()=>navigateTo(`/invoices/${i.id}`)}>Chi tiết</button></td></tr>)}</tbody></table></div></div>}
