import{useEffect,useState}from'react';import{cancelInvoice,getInvoice,issueInvoice}from'../payments/api';import type{PaymentInvoice}from'../payments/types';import{formatApiError}from'../../services/apiClient';const money=(v:number)=>new Intl.NumberFormat('vi-VN',{style:'currency',currency:'VND',maximumFractionDigits:0}).format(v);export function InvoiceDetailPage({invoiceId}:{invoiceId:string}){const[item,setItem]=useState<PaymentInvoice|null>(null);const[errors,setErrors]=useState<string[]>([]);async function load(){setItem((await getInvoice(invoiceId)).data)}useEffect(()=>{void load()},[invoiceId]);async function issue(){try{await issueInvoice(invoiceId,{});await load()}catch(e){setErrors(formatApiError(e))}}async function cancel(){const reason=window.prompt('Nhập lý do hủy hóa đơn');if(!reason)return;try{await cancelInvoice(invoiceId,{cancel_reason:reason});await load()}catch(e){setErrors(formatApiError(e))}}if(!item)return <div>Đang tải…</div>;return <div className="admin-page print-page"><header className="page-header no-print"><div><h1>Hóa đơn {item.invoice_code}</h1><p>{item.status_label}</p></div>{item.status==='draft'&&<button onClick={issue}>Phát hành hóa đơn</button>}{item.status!=='cancelled'&&<button className="secondary-button" onClick={cancel}>Hủy hóa đơn</button>}<button onClick={()=>window.print()}>In hóa đơn</button></header>{errors.length>0&&<div className="form-error no-print">{errors.join('. ')}</div>}<section className="detail-card"><h2>Hóa đơn</h2><dl className="info-grid"><div><dt>Mã hóa đơn</dt><dd>{item.invoice_code}</dd></div><div><dt>Trạng thái</dt><dd>{item.status_label}</dd></div><div><dt>Số tiền</dt><dd>{money(item.amount)}</dd></div><div><dt>Ngày phát hành</dt><dd>{item.issued_date?new Date(item.issued_date).toLocaleDateString('vi-VN'):'Chưa phát hành'}</dd></div><div><dt>Hợp đồng</dt><dd>{item.contract?.contract_code||'—'}</dd></div><div><dt>Phiếu thu</dt><dd>{item.receipt?.receipt_code||'—'}</dd></div><div><dt>Lịch thanh toán</dt><dd>{item.payment_schedule?.payment_code} · {item.payment_schedule?.title}</dd></div><div><dt>Khách hàng</dt><dd>{item.customer?.full_name||'—'}</dd></div><div><dt>BĐS</dt><dd>{item.property?.property_code||'—'} · {item.property?.title||''}</dd></div><div><dt>Mô tả</dt><dd>{item.description||'—'}</dd></div><div><dt>Ghi chú</dt><dd>{item.note||'—'}</dd></div><div><dt>Lý do hủy</dt><dd>{item.cancel_reason||'—'}</dd></div></dl></section></div>}
