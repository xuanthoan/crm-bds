import { useState,type FormEvent } from 'react'; import { Modal } from '../../components/Modal'; import { FormError } from '../../components/FormError'; import { formatApiError } from '../../services/apiClient'; import type { AdminUser } from '../admin/users/api'; import type { Department,Membership,MembershipPayload,Team } from './types';
export function MembershipFormModal({item,departments,teams,users,onClose,onSubmit}:{item:Membership|null;departments:Department[];teams:Team[];users:AdminUser[];onClose:()=>void;onSubmit:(p:MembershipPayload)=>Promise<void>}) { const [p,setP]=useState<MembershipPayload>({user_id:item?.user.id??'',department_id:item?.department.id??'',team_id:item?.team?.id??'',position_title:item?.position_title??'',is_primary:item?.is_primary??true}); const [errors,setErrors]=useState<string[]|null>(null); function selectTeam(id:string){const team=teams.find(t=>t.id===id);setP({...p,team_id:id,department_id:team?.department.id??p.department_id})} async function submit(e:FormEvent){e.preventDefault();try{await onSubmit({...p,team_id:p.team_id||null})}catch(err){setErrors(formatApiError(err))}} return <Modal title={item?'Sửa phân bổ':'Gán nhân sự'} onClose={onClose}><form className="admin-form" onSubmit={submit}>{!item&&<label>Nhân sự<select value={p.user_id} onChange={(e:any)=>setP({...p,user_id:e.target.value})} required><option value="">Chọn nhân sự</option>{users.map(u=><option key={u.id} value={u.id}>{u.full_name} ({u.roles.map(r=>r.code).join(', ')})</option>)}</select></label>}<label>Phòng ban<select value={p.department_id??''} onChange={(e:any)=>setP({...p,department_id:e.target.value,team_id:''})} required><option value="">Chọn phòng ban</option>{departments.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select></label><label>Nhóm<select value={p.team_id??''} onChange={(e:any)=>selectTeam(e.target.value)}><option value="">Không chọn nhóm</option>{teams.filter(t=>!p.department_id||t.department.id===p.department_id).map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></label><label>Chức danh<input value={p.position_title??''} onChange={(e:any)=>setP({...p,position_title:e.target.value})}/></label><label className="checkbox-row"><input type="checkbox" checked={p.is_primary} onChange={(e:any)=>setP({...p,is_primary:e.target.checked})}/>Phân bổ chính</label><FormError messages={errors}/><footer className="modal-actions"><button type="button" className="secondary-button" onClick={onClose}>Hủy</button><button>Lưu</button></footer></form></Modal> }
