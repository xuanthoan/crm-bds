import type { Department, Team } from '../types';
export function OrganizationFilters({departments,teams,departmentId,teamId,onDepartment,onTeam}:{departments:Department[];teams:Team[];departmentId:string;teamId:string;onDepartment:(v:string)=>void;onTeam:(v:string)=>void}) { return <div className="filter-panel"><select value={departmentId} onChange={(e:any)=>onDepartment(e.target.value)}><option value="">Tất cả phòng ban</option>{departments.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select><select value={teamId} onChange={(e:any)=>onTeam(e.target.value)}><option value="">Tất cả nhóm</option>{teams.filter(t=>!departmentId||t.department.id===departmentId).map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></div>; }
