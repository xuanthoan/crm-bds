import { useEffect,useState } from 'react'; import { Badge } from '../../components/Badge'; import { ConfirmDialog } from '../../components/ConfirmDialog'; import { FormError } from '../../components/FormError'; import { formatApiError } from '../../services/apiClient'; import { listUsers,type AdminUser } from '../admin/users/api'; import { createDepartment,deleteDepartment,listDepartments,updateDepartment } from './api'; import { DepartmentFormModal } from './DepartmentFormModal'; import type { Department,DepartmentPayload } from './types';
export function DepartmentsPage(){const [items,setItems]=useState<Department[]>([]),[users,setUsers]=useState<AdminUser[]>([]),[edit,setEdit]=useState<Department|null|undefined>(),[remove,setRemove]=useState<Department|null>(null),[errors,setErrors]=useState<string[]|null>(null);async function load(){try{const [a,b]=await Promise.all([listDepartments(),listUsers({page_size:100,status:'active'})]);setItems(a.data);setUsers(b.data)}catch(e){setErrors(formatApiError(e))}}useEffect(()=>{void load()},[]);async function save(p:DepartmentPayload){edit?await updateDepartment(edit.id,p):await createDepartment(p);setEdit(undefined);await load()}return <section className="admin-page"><header className="page-header"><div><p className="eyebrow">Quản trị hệ thống</p><h1>Quản lý phòng ban</h1></div><button onClick={()=>setEdit(null)}>Tạo phòng ban</button></header><FormError messages={errors}/><div className="table-card"><table><thead><tr><th>Tên phòng ban</th><th>Mã</th><th>Quản lý</th><th>Trạng thái</th><th>Số team</th><th>Số nhân sự</th><th>Ngày tạo</th><th>Thao tác</th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td>{i.name}</td><td><code>{i.code}</code></td><td>{i.manager?.full_name??'—'}</td><td><Badge tone={i.status==='active'?'green':'gray'}>{i.status==='active'?'Hoạt động':'Không hoạt động'}</Badge></td><td>{i.team_count}</td><td>{i.member_count}</td><td>{new Date(i.created_at).toLocaleDateString('vi-VN')}</td><td><div className="action-cell"><button className="link-button" onClick={()=>setEdit(i)}>Sửa</button><button className="link-button danger-link" onClick={()=>setRemove(i)}>Vô hiệu hóa</button></div></td></tr>)}</tbody></table></div>{edit!==undefined&&<DepartmentFormModal item={edit} users={users} onClose={()=>setEdit(undefined)} onSubmit={save}/>} {remove&&<ConfirmDialog title="Vô hiệu hóa phòng ban" message={`Vô hiệu hóa ${remove.name}?`} confirmLabel="Vô hiệu hóa" onCancel={()=>setRemove(null)} onConfirm={async()=>{try{await deleteDepartment(remove.id);setRemove(null);await load()}catch(e){setErrors(formatApiError(e));setRemove(null)}}}/>}</section>}
