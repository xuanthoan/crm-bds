import{PROJECT_STATUS_LABELS,PROJECT_TYPE_LABELS}from'../constants';import type{ProjectFilters as Values}from'../types';
export function ProjectFilters({value,onChange}:{value:Values;onChange:(v:Values)=>void}){const set=(k:keyof Values,v:string)=>onChange({...value,[k]:v,page:1});return <div className="inventory-filters"><input placeholder="Tìm mã, tên, chủ đầu tư, địa chỉ" value={value.q||''} onChange={e=>set('q',e.target.value)}/><input placeholder="Tỉnh/Thành phố" value={value.province||''} onChange={e=>set('province',e.target.value)}/><input placeholder="Quận/Huyện" value={value.district||''} onChange={e=>set('district',e.target.value)}/><select value={value.project_type||''} onChange={e=>set('project_type',e.target.value)}><option value="">Tất cả loại dự án</option>{Object.entries(PROJECT_TYPE_LABELS).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select><select value={value.status||''} onChange={e=>set('status',e.target.value)}><option value="">Tất cả trạng thái</option>{Object.entries(PROJECT_STATUS_LABELS).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></div>}
