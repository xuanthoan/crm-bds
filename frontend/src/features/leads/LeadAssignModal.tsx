import { FormEvent, useState } from 'react'; import { FormError } from '../../components/FormError'; import { Modal } from '../../components/Modal'; import { formatApiError } from '../../services/apiClient'; import type { AdminUser } from '../admin/users/api';
export function LeadAssignModal({ owners,onClose,onSubmit }:{owners:AdminUser[];onClose:()=>void;onSubmit:(id:string,note?:string)=>Promise<void>}){const [owner,setOwner]=useState(''),[note,setNote]=useState(''),[errors,setErrors]=useState<string[]|null>(null);async function submit(e:FormEvent){e.preventDefault();try{await onSubmit(owner,note||undefined)}catch(error){setErrors(formatApiError(error))}}return <Modal title="Phân công lead" onClose={onClose}><form className="admin-form" onSubmit={submit}><label>Người phụ trách<select value={owner} onChange={(e:any)=>setOwner(e.target.value)} required><option value="">Chọn người phụ trách</option>{owners.map(u=><option key={u.id} value={u.id}>{u.full_name} ({u.email})</option>)}</select></label><label>Ghi chú<textarea value={note} onChange={(e:any)=>setNote(e.target.value)}/></label><FormError messages={errors}/><footer className="modal-actions"><button type="button" className="secondary-button" onClick={onClose}>Hủy</button><button>Phân công</button></footer></form></Modal>}
