import type{BookingActivity}from'../types';export function BookingTimeline({activities}:{activities:BookingActivity[]}){return <div className="lead-timeline">{activities.map(a=><article className="timeline-item" key={a.id}><div><header><h4>{a.title}</h4><time>{new Date(a.created_at).toLocaleString('vi-VN')}</time></header><p>{a.content||a.activity_label}</p>{a.old_value!==null||a.new_value!==null?<small>{a.old_value||'—'} → {a.new_value||'—'}</small>:null}<small>{a.actor.full_name}</small></div></article>)}{!activities.length&&<p className="empty-state">Chưa có hoạt động.</p>}</div>}
