import{BOOKING_STATUS_LABELS}from'../constants';import type{BookingFilters as Filters,BookingUser}from'../types';
export function BookingFilters({value,assignees,onChange}:{value:Filters;assignees:BookingUser[];onChange:(value:Filters)=>void}){const set=(key:keyof Filters,v:string)=>onChange({...value,[key]:v,page:1});return <div className="inventory-filters booking-filters"><input placeholder="Mã booking, khách hàng, bất động sản" value={value.q||''} onChange={e=>set('q',e.target.value)}/><select value={value.status||''} onChange={e=>set('status',e.target.value)}><option value="">Tất cả trạng thái</option>{Object.entries(BOOKING_STATUS_LABELS).map(([k,v])=><option key={k} value={k}>{v}</option>)}</select><select value={value.assigned_user_id||''} onChange={e=>set('assigned_user_id',e.target.value)}><option value="">Tất cả người phụ trách</option>{assignees.map(u=><option key={u.id} value={u.id}>{u.full_name}</option>)}</select><label>Tạo từ<input type="date" value={value.created_from||''} onChange={e=>set('created_from',e.target.value)}/></label><label>Tạo đến<input type="date" value={value.created_to||''} onChange={e=>set('created_to',e.target.value)}/></label><label>Hết hạn từ<input type="date" value={value.expires_from||''} onChange={e=>set('expires_from',e.target.value)}/></label><label>Hết hạn đến<input type="date" value={value.expires_to||''} onChange={e=>set('expires_to',e.target.value)}/></label></div>}
