import{useState,type FormEvent}from'react';import{formatApiError}from'../../services/apiClient';import{addBookingActivity}from'./api';export function BookingActivityForm({bookingId,onSaved}:{bookingId:string;onSaved:()=>void}){const[title,setTitle]=useState('');const[content,setContent]=useState('');const[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();if(!title.trim())return setError('Tiêu đề là bắt buộc');try{await addBookingActivity(bookingId,{title,content});setTitle('');setContent('');onSaved()}catch(err){setError(formatApiError(err).join('. '))}}return <form className="property-form" onSubmit={submit}><label>Tiêu đề<input value={title} onChange={e=>setTitle(e.target.value)}/></label><label>Nội dung<textarea value={content} onChange={e=>setContent(e.target.value)}/></label>{error&&<div className="form-error">{error}</div>}<button>Thêm ghi chú</button></form>}
