import {TASK_PRIORITY_LABELS,TASK_STATUS_LABELS} from '../constants'; export function TaskBadge({value,kind='status'}:{value:string;kind?:'status'|'priority'}){const red=value==='urgent'||value==='cancelled',orange=value==='high'||value==='in_progress',green=value==='completed';return <span className={`badge ${red?'badge-red':orange?'badge-orange':green?'badge-green':'badge-blue'}`}>{(kind==='status'?TASK_STATUS_LABELS:TASK_PRIORITY_LABELS)[value]??value}</span>}
