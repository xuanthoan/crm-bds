import{useState,type FormEvent}from'react';import{Modal}from'../../components/Modal';import{FormError}from'../../components/FormError';import{formatApiError}from'../../services/apiClient';import{TASK_PRIORITIES,TASK_TYPES}from'./constants';import type{LeadTask,TaskPayload}from'./types';
const local=(v?:string)=>v?new Date(v).toISOString().slice(0,16):'';export function TaskFormModal({leadId,ownerId,item,onClose,onSubmit}:{leadId:string;ownerId?:string|null;item?:LeadTask|null;onClose:()=>void;onSubmit:(p:TaskPayload)=>Promise<void>}){const[p,setP]=useState<TaskPayload>({lead_id:leadId,title:item?.title??'',description:item?.description,task_type:item?.task_type??'call',priority:item?.priority??'medium',due_at:local(item?.due_at),assigned_to_id:item?.assigned_to.id??ownerId,reminder_enabled:item?.reminder_enabled??true,reminder_at:local(item?.reminder_at??undefined)}),[errors,setErrors]=useState<string[]|null>(null);async function submit(e:FormEvent){e.preventDefault();try{await onSubmit({...p,due_at:new Date(p.due_at).toISOString(),reminder_at:p.reminder_at?new Date(p.reminder_at).toISOString():null})}catch(x){setErrors(formatApiError(x))}}return <Modal title={item?'Sửa công việc':'Tạo công việc'} onClose={onClose}><form className="admin-form" onSubmit={submit}><label>Tiêu đề<input value={p.title} onChange={(e:any)=>setP({...p,title:e.target.value})} required/></label><label>Loại<select value={p.task_type} onChange={(e:any)=>setP({...p,task_type:e.target.value})}>{Object.entries(TASK_TYPES).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label>Ưu tiên<select value={p.priority} onChange={(e:any)=>setP({...p,priority:e.target.value})}>{Object.entries(TASK_PRIORITIES).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label>Hạn xử lý<input type="datetime-local" value={p.due_at} onChange={(e:any)=>setP({...p,due_at:e.target.value})} required/></label><label>Nhắc lúc<input type="datetime-local" value={p.reminder_at??''} onChange={(e:any)=>setP({...p,reminder_at:e.target.value})}/></label><label>Mô tả<textarea value={p.description??''} onChange={(e:any)=>setP({...p,description:e.target.value})}/></label><FormError messages={errors}/><footer className="modal-actions"><button type="button" className="secondary-button" onClick={onClose}>Hủy</button><button>Lưu</button></footer></form></Modal>}
