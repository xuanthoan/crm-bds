import{useState,type FormEvent}from'react';import{Modal}from'../../components/Modal';export function TaskStatusModal({status,onClose,onSubmit}:{status:string;onClose:()=>void;onSubmit:(note:string)=>Promise<void>}){const[note,setNote]=useState('');return <Modal title={status==='completed'?'Hoàn thành công việc':'Hủy công việc'} onClose={onClose}><form className="admin-form" onSubmit={async(e:FormEvent)=>{e.preventDefault();await onSubmit(note)}}><label>Kết quả / lý do<textarea value={note} onChange={(e:any)=>setNote(e.target.value)}/></label><footer className="modal-actions"><button type="button" className="secondary-button" onClick={onClose}>Đóng</button><button>Xác nhận</button></footer></form></Modal>}
