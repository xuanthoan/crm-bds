import {useState} from 'react';import {Modal} from '../../components/Modal';export function TaskStatusModal({status,onClose,onSubmit}:{status:'done'|'cancelled';onClose:()=>void;onSubmit:(note:string)=>Promise<void>}){const [note,setNote]=useState('');return <Modal title={status==='done'?'Hoàn thành công việc':'Hủy công việc'} onClose={onClose}><form className="admin-form" onSubmit={e=>{e.preventDefault();void onSubmit(note)}}><label>Ghi chú kết quả<textarea value={note} onChange={e=>setNote(e.target.value)}/></label><footer className="modal-actions"><button type="button" className="secondary-button" onClick={onClose}>Đóng</button><button type="submit">Xác nhận</button></footer></form></Modal>}
