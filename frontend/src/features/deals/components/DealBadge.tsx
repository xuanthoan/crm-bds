import {Badge} from '../../../components/Badge'; import {DEAL_PRIORITY_LABELS,DEAL_STATUS_LABELS,PIPELINE_STAGE_LABELS} from '../constants'; import type {DealPriority,DealStage,DealStatus} from '../types';
export function DealBadge({stage,status,priority}:{stage?:DealStage;status?:DealStatus;priority?:DealPriority}){const value=stage?PIPELINE_STAGE_LABELS[stage]:status?DEAL_STATUS_LABELS[status]:priority?DEAL_PRIORITY_LABELS[priority]:'';const tone=status==='won'||stage==='completed'?'green':status==='lost'||status==='cancelled'||stage==='lost'?'red':priority==='urgent'?'red':stage==='deposit'||stage==='contract'||stage==='contract_signed'||priority==='high'?'orange':'blue';return <Badge tone={tone}>{value}</Badge>}
