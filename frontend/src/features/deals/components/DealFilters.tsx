import {DEAL_PRIORITY_LABELS,DEAL_STATUS_LABELS,DEAL_TYPE_LABELS,PIPELINE_STAGE_LABELS} from '../constants'; import type {DealFilters as Filters} from '../api';
export function DealFilters({filters,onChange}:{filters:Filters;onChange:(v:Filters)=>void}){const select=(key:keyof Filters,labels:Record<string,string>,placeholder:string)=><select value={String(filters[key]??'')} onChange={e=>onChange({...filters,[key]:e.target.value,page:1})}><option value="">{placeholder}</option>{Object.entries(labels).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select>;return <div className="filter-row work-filters deal-filters"><input placeholder="Mã giao dịch, tên, khách hàng, số điện thoại" value={filters.q??''} onChange={e=>onChange({...filters,q:e.target.value,page:1})}/>{select('pipeline_stage',PIPELINE_STAGE_LABELS,'Tất cả giai đoạn')}{select('status',DEAL_STATUS_LABELS,'Tất cả trạng thái')}{select('priority',DEAL_PRIORITY_LABELS,'Tất cả ưu tiên')}{select('deal_type',DEAL_TYPE_LABELS,'Tất cả loại')}</div>}
