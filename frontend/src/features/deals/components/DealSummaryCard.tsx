import type {Deal} from '../types';const money=(v:string|number|null)=>v==null?'—':new Intl.NumberFormat('vi-VN').format(Number(v))+' ₫';export function DealSummaryCard({deal}:{deal:Deal}){return <section className="detail-card"><h2>Giá trị giao dịch</h2><dl className="info-grid"><div><dt>Giá trị dự kiến</dt><dd>{money(deal.expected_value)}</dd></div><div><dt>Tiền đặt cọc</dt><dd>{money(deal.deposit_amount)}</dd></div><div><dt>Giá trị hợp đồng</dt><dd>{money(deal.contract_value)}</dd></div><div><dt>Hoa hồng dự kiến</dt><dd>{money(deal.commission_expected)}</dd></div></dl></section>}
