import {ACTIVITY_LABELS} from '../constants';import type {DealActivity} from '../types';export function DealTimeline({activities=[]}:{activities?:DealActivity[]}){return <div className="lead-timeline">{activities.map(a=><article className="timeline-item" key={a.id}><span className="timeline-marker"/><div><header><strong>{ACTIVITY_LABELS[a.activity_type as keyof typeof ACTIVITY_LABELS]||a.activity_type}</strong><time>{new Date(a.created_at).toLocaleString('vi-VN')}</time></header><h4>{a.title}</h4>{a.content&&<p>{a.content}</p>}{(a.old_value||a.new_value)&&<span className="value-change">{a.old_value||'—'} → {a.new_value||'—'}</span>}<small>{a.user?.full_name||'Người dùng hệ thống'}</small></div></article>)}{!activities.length&&<p className="empty-state">Chưa có hoạt động.</p>}</div>}
