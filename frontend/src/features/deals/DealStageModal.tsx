import {useState} from 'react';import {Modal} from '../../components/Modal';import {FormError} from '../../components/FormError';import {formatApiError} from '../../services/apiClient';import {changeDealStage} from './api';import {PIPELINE_STAGE_LABELS} from './constants';import type {Deal,DealStage} from './types';export function DealStageModal({deal,onClose,onSaved}:{deal:Deal;onClose:()=>void;onSaved:()=>void}){const [stage,setStage]=useState<DealStage>(deal.pipeline_stage);const [note,setNote]=useState('');const [amount,setAmount]=useState('');const [date,setDate]=useState('');const [reason,setReason]=useState('');const [errors,setErrors]=useState<string[]>([]);async function submit(e:any){e.preventDefault();if(stage==='lost'&&!reason.trim()){setErrors(['Vui lòng nhập lý do thất bại/hủy giao dịch']);return}try{await changeDealStage(deal.id,{pipeline_stage:stage,note:note.trim()||null,deposit_amount:stage==='deposit'&&amount?Number(amount):null,deposit_date:stage==='deposit'&&date?new Date(date).toISOString():null,contract_value:stage==='contract'&&amount?Number(amount):null,contract_date:stage==='contract'&&date?new Date(date).toISOString():null,lost_reason:stage==='lost'?reason:null});onSaved()}catch(err){setErrors(formatApiError(err))}}return <Modal title="Đổi giai đoạn" onClose={onClose}><form onSubmit={submit}><FormError messages={errors}/><div className="form-grid"><label>Giai đoạn<select value={stage} onChange={e=>setStage(e.target.value as DealStage)}>{Object.entries(PIPELINE_STAGE_LABELS).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label>{(stage==='deposit'||stage==='contract')&&<><label>{stage==='deposit'?'Tiền đặt cọc':'Giá trị hợp đồng'}<input type="number" min="0" value={amount} onChange={e=>setAmount(e.target.value)}/></label><label>Ngày ghi nhận<input type="datetime-local" value={date} onChange={e=>setDate(e.target.value)}/></label></>}{stage==='lost'&&<label className="full-span">Lý do thất bại / hủy *<textarea required value={reason} onChange={e=>setReason(e.target.value)}/></label>}<label className="full-span">Ghi chú<textarea value={note} onChange={e=>setNote(e.target.value)}/></label></div><footer className="modal-actions"><button type="button" className="secondary-button" onClick={onClose}>Hủy</button><button>Lưu</button></footer></form></Modal>}
