import{APPOINTMENT_STATUSES,APPOINTMENT_TYPES}from'../constants';import type{AppointmentFilters as F}from'../api';export function AppointmentFilters({filters,onChange,onSearch}:{filters:F;onChange:(f:F)=>void;onSearch:()=>void}){return <div className="filter-panel"><select value={filters.status??''} onChange={(e:any)=>onChange({...filters,status:e.target.value})}><option value="">Tất cả trạng thái</option>{Object.entries(APPOINTMENT_STATUSES).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select><select value={filters.appointment_type??''} onChange={(e:any)=>onChange({...filters,appointment_type:e.target.value})}><option value="">Tất cả loại</option>{Object.entries(APPOINTMENT_TYPES).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select><input type="datetime-local" value={filters.start_from??''} onChange={(e:any)=>onChange({...filters,start_from:e.target.value})}/><input type="datetime-local" value={filters.start_to??''} onChange={(e:any)=>onChange({...filters,start_to:e.target.value})}/><button onClick={onSearch}>Lọc</button></div>}
