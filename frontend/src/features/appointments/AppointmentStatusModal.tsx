import {useState} from 'react';import {Modal} from '../../components/Modal';import type {AppointmentStatus} from './types';export function AppointmentStatusModal({status,onClose,onSubmit}:{status:AppointmentStatus;onClose:()=>void;onSubmit:(note:string,start?:string,end?:string)=>Promise<void>}){const [note,setNote]=useState('');const [start,setStart]=useState('');const [end,setEnd]=useState('');return <Modal title={status==='rescheduled'?'Dời lịch hẹn':'Cập nhật lịch hẹn'} onClose={onClose}><form className="admin-form" onSubmit={e=>{e.preventDefault();void onSubmit(note,start?new Date(start).toISOString():undefined,end?new Date(end).toISOString():undefined)}}>{status==='rescheduled'&&<div className="form-grid"><label>Bắt đầu mới<input type="datetime-local" required value={start} onChange={e=>setStart(e.target.value)}/></label><label>Kết thúc mới<input type="datetime-local" value={end} onChange={e=>setEnd(e.target.value)}/></label></div>}<label>Ghi chú kết quả<textarea value={note} onChange={e=>setNote(e.target.value)}/></label><footer className="modal-actions"><button type="button" className="secondary-button" onClick={onClose}>Đóng</button><button type="submit">Xác nhận</button></footer></form></Modal>}
